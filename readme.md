# UnArxiv

UnArxiv is a finetuning project with Qwen 2.5 3B for simplifying research paper abstracts. The project utilizes Intel Arc GPUs locally instead of the conventional and widely used Nvidia GPUs with CUDA support.
